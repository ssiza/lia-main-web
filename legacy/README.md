# Legacy static site

Pre–Next.js HTML, CSS, JS, and `img/` assets. The live site is the Next.js app in `app/` and `public/`.

Open files here directly in a browser only for reference; paths are relative to this folder (`img/`, shared styles).
